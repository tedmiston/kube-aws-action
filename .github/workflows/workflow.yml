name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello actions
      - name: Docker build
        run: docker build . --file Dockerfile --tag tedmiston/kube-aws-action:$(date +%s)
